# Basics of R programming

print("Hello World")
x <- 20
x

# Variables and R is case sensitive
X =89
print(X)

# Assigning values from 1 to 10 in both a and b
a <- b <- (1:10)

# Adding values of a and b elements and storing them in the variable c
# Multiplying the values of a and b elements and storing them in tha variables c

c <- a+b
d <- a*b

# concentenation the values
e <- c(1,2,3,5,6)

# Removing the element from the workspace
rm (e)

# Removing all the elements from the workspace in single go
rm (list=ls())

# listing all the elements present in the workspace
ls()
